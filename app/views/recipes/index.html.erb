<!DOCTYPE html>
<html>
<head>
  <title>User Posts</title>
</head>
  <body>
  <div class="flex-column-centered  ">
  <div class="flex-spaced">
    <p>&nbsp;</p>
    <p class="recipe-name large ">
      Recipes
    </p>
    <div>
      <%= link_to new_recipe_path(@user), class: "plain" do %>
        <p  class="btn-2"> Add New </p>
        <% end  %>
    </div>
  </div>
  <div class="flex-column-centered w-100 mt-m1">  
  <%  @recipes.each do |recipe| %>
    <% if (recipe.public?) || recipe.user == current_user %>
        <%= link_to user_recipe_path(id: recipe.id, user_id: recipe.user.id), class: "plain" do %>
          <div class="flex-spaced bordered gap-3 p-1">
            <div class="flex-column-start gap-c3">
              <p class="recipe-name">
                <%= recipe.name %>
              </p>
              <% if can? :destroy, recipe %>
              <%= form_with(model: @recipe, url: recipe_path(@recipe, id: recipe.id), method: :delete, data: { turbo: "false" }) do |form| %>
                <%= form.submit "delete", class: "flex-start", data: { confirm: "Are you sure you want to delete this food?" } %>
              <% end %>
              <% end %>
            </div>
            <div class="desc-scroll">
              <%= recipe.description %>
          </div>
          </div>
        <% end %>
        <% end %>
        <% end %>
        </div>
  </body>
</html>
